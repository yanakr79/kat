/*
const uk = require('../../content/data/translations/src/translations.uk.json');
const en = require('../../content/data/translations/src/translations.en.json');
const ru = require('../../content/data/translations/src/translations.ru.json');

const getTrans = (locale) => {
  switch (locale) {
    case 'uk':
      return uk;
    case 'en':
      return en;
    case 'ru':
      return ru;
    default:
      throw new Error(`Unknown locale ${locale}`);
  }
};

module.exports = getTrans;
*/
